[pytest]
pythonpath = .
testpaths = tests
markers =
    unit: fast unit-level tests
    integration: parser/normalizer/rendering integration tests
    pdf: tests that render or inspect PDF output
    slow: slower end-to-end or file-heavy tests
addopts = -ra
