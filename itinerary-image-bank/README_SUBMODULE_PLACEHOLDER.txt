This folder is represented as a Git submodule in GitHub.

After PR #26 is merged and the repo is cloned with submodules, Git will populate this folder from:
https://github.com/Dkaalen/itinerary-image-bank.git

This placeholder is only included so the edited-files zip shows the expected path.
